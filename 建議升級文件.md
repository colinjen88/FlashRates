# Goldlab.cloud / FlashRates - v4.0 建議升級與優化指南

這份文件記錄了自目前 `v2.9` 版本至 `v4.0` 主分支 (main) 中，針對**效能**、**安全性**與**系統穩定性**所做的核心更新與修改。在您後續自行優化並套用現有版面時，建議將以下核心邏輯逐步實作至目前的程式碼中。

---

## 一、 安全性強化 (API Key 保護)

在舊版架構中，管理頁面可能包含部份寫死 (Hardcoded) 的憑證，且 API 大多為開放狀態或單一通行碼。

### 建議後續實作項目：
1. **移除前端硬編碼憑證**：
   - 移除 `App.jsx` 或 `LoginModal.jsx` 中的明碼 `username` 與 `password` (如 `Goldlab.WANG1`)。
   - 改由後端的 `ADMIN_API_KEYS`，結合登入時動態換發 Token 或是直接使用有效的 API Key。
2. **多重 API Key 驗證整合 (Frontend)**：
   - 更新前端對 WebSocket 和 REST API 取得 Token 的彈性。可依序由下列取得：
     ```javascript
     const apiKey = urlParamKey || adminLoginKey || localStorage.getItem("goldlab_api_key") || import.meta.env.VITE_PUBLIC_API_KEY;
     ```
   - 確保所有向後端發送的 Request 都帶上 `Authorization: Bearer <API_KEY>` 或是 `X-API-Key: <API_KEY>` Header。
3. **Public Read 路由匿名存取 (Backend)**：
   - 若為對外大眾公開使用的實時報價版面 (`/api/v1/latest`, `/api/v1/history`, `/ws/stream`)，後端路由應該實作允許匿名存取的防護網 (或使用固定 Public Key) 以避免非法濫用，但保留白名單機制供 Dashboard 直接存取。

---

## 二、 系統穩定性與報價容錯追蹤

報價常遇第三方來源連線失敗、延遲過長，或者單一來源失效。在 v4.0 中已做了諸多容錯與警告訊息改善。

### 建議後續實作項目：
1. **整合幣安 (Binance) Fallback 機制**：
   - 確保在 XAU/XAG (黃金/白銀) 常規抓取點失效或延遲過高時，可以依賴 Binance / USDT 交易對與美金匯率 (USD-TWD) 進行反推或作為穩定來源 Fallback。
2. **來源失效警示 (Source Warnings)**：
   - 將「長時間無更新」或「被剔除的資料源」等特定標記呈現於 UI 或 `LogModal` 中，使管理者能第一時間得知哪個 Scraper 失效。
3. **歷史價格序列化 (Sparklines 歷史順序)**：
   - 實作或調整 `/api/v1/history?limit=50` 呼叫，並注意取得 Array 時，Redis ZREVRANGE 的順序為「由新到舊」。若繪製迷你折線圖 (Sparkline) 為從左(舊)到右(新)，記得在取得資料後實作 `.reverse()`。

---

## 三、 效能優化與架構 (Performance)

1. **模組化分流與重新渲染**：
   - 目前的 `v2.9` 儀表板已經有很好的結構區分，但是所有報價狀態 (如 `marketData`, `prevMarketData`, `priceHistory`) 都集中於父組件不斷地 SetState，這在高頻報價 (如 80ms 延遲) 下會導致整個頁面頻繁渲染。
   - 建議可實作更小單位的 `Context Provider` 或是由單獨的 Component (例如個別的 `AssetCard`) 自行負責訂閱更新與監聽對應的 Symbol，以降低 React DOM 重繪負載。
2. **WebSocket 連線狀態管理 (Lifecycle)**：
   - 對於 `ws.onclose` 的斷線重連機制，增加遞增補償 (Exponential Backoff)，以防止伺服器重啟過渡期間，大量 Browser Node 產生廣播風暴壓垮後端伺服器 (Reconnect Timeout 從 3000ms 逐步遞增)。
3. **聚合層效能改進 (Backend Redis)**：
   - Rate Limiting (限流) 以及 Metrics 完全引入 Redis 進行持久化，支援 PM2 或 Docker 多負載平衡。

---

## 結語
目前的 `v2.9` 排版擁有最佳的多區分段配置，只要在現有外觀下，為 `DashboardSection.jsx` 和 `AssetCard.jsx` 補回上述 **Token 提取**與**歷史陣列 (.reverse)** 的串接邏輯，就能結合這個完美排版以及 `v4.0` 最新後端支援！
